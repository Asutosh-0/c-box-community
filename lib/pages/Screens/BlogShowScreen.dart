import 'package:flutter/cupertino.dart';

class BlogShowScreen extends StatelessWidget {
  const BlogShowScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Blog show screen"),
    );
  }
}
